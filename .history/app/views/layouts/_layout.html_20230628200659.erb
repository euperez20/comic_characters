

<!DOCTYPE html>
<html>
  <head>
    <title>Marvel Character List</title>
    <!-- Importa los archivos CSS de Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
  </head>
  <body>
    <!-- Menú de navegación -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <%= link_to "Comic Characters", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= link_to "Characters", characters_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Publishers", publishers_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Origins", origins_path, class: "nav-link" %>
        </li>
      </ul>

      <%= form_tag characters_path, method: :get, class: "form-inline my-2 my-lg-0" do %>
        <input class="form-control mr-sm-2" type="search" placeholder="Search" name="search" value="<%= params[:search] %>">
        <%= submit_tag "Search", class: "btn btn-outline-primary my-2 my-sm-0" %>
      <% end %>

      <%= form_tag characters_path, method: :get, class: "form-inline my-2 my-lg-0" do %>
        <%= select_tag "origin_id", options_from_collection_for_select(Origin.all, "id", "name", params[:origin_id]), prompt: "Select Origin", class: "form-control mr-sm-2" %>
        <%= submit_tag "Filter", class: "btn btn-outline-primary my-2 my-sm-0" %>
      <% end %>
    </div>
  </div>
</nav>
    <!-- Contenido de la página -->
    <div class="container">
      <%= yield %>
    </div>

    <!-- Importa los archivos JS de Bootstrap al final del documento -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
